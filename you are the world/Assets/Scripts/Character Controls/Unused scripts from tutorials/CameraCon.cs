using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraCon : MonoBehaviour
{

    /*
     * followed this from a tutorial
     * i dont think it will be used but the Lerp thing adds a interesting trail
     * not rigid glued to the character but smoother follow so i wanna keep the code to look at later.
     * 
    public Transform oreintation;
    public float pLerp = .02f;
    public float rLerp = .01f;

    private void Update() {
        transform.position = Vector3.Lerp(transform.position, oreintation.position, pLerp);
        transform.rotation = Quaternion.Lerp(transform.rotation,oreintation.rotation, rLerp);
    }
    */
}
